mkdir /foo
git clone <path>
apt-get update -y
apt-get install -y npm nodejs procps
npm install 
node server.js &
ps -ef

Above will run server on port 8080
curl localhost:8080

####
PATHS:
/ -> helloworld
/health -> healthy server
/foo -> foo endpoint
